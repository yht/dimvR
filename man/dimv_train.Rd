% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimv.R
\name{dimv_train}
\alias{dimv_train}
\title{Train DIMV Imputer (Ridge-Conditional)}
\usage{
dimv_train(
  X,
  lambda = 1,
  maxit = 50,
  tol = 1e-04,
  feature_select = FALSE,
  adaptive = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{X}{A numeric data frame or matrix (may contain \code{NA}).}

\item{lambda}{Base ridge penalty (default \code{1.0}).}

\item{maxit}{Maximum number of iterations (default \code{50}).}

\item{tol}{Convergence threshold for RMSE of updated missing values (default \code{1e-4}).}

\item{feature_select}{Logical; if \code{TRUE}, apply per-variable feature selection (default \code{FALSE}).}

\item{adaptive}{Logical; if \code{TRUE}, use adaptive lambda per variable (default \code{FALSE}).}

\item{verbose}{If \code{TRUE}, prints iteration logs.}
}
\value{
A \code{dimv_imputer} object containing fitted models, column means,
residual variances, convergence \code{diff_history}, and metadata.
}
\description{
Trains a conditional ridge regression model for each variable to impute
missing values. Missing entries are first initialized using column means,
and then iteratively refined until convergence. Optionally supports
per-variable feature selection (using column indexes) and adaptive
regularization (both disabled by default).
}
\examples{
set.seed(1)
X <- data.frame(a = rnorm(50), b = rnorm(50), c = rnorm(50))
for (j in seq_along(X)) X[sample(1:nrow(X), 5), j] <- NA
imp <- dimv_train(X, lambda = 0.5)
print(imp)

}
